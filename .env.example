# Anthropic
ANTHROPIC_API_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_SOLO=
STRIPE_PRICE_PRO=
STRIPE_PRICE_TEAM=

# Transcription (optional for v1 — paste-only works without it)
WHISPER_API_KEY=

# Database (Supabase Postgres URL)
DATABASE_URL=

# Admin gate — comma-separated Supabase auth user UUIDs (visible in
# Supabase dashboard → Authentication → Users). Anyone in this list sees
# the /admin page and the founder review queue.
ADMIN_USER_IDS=
