# Multimodal AI for paper-to-digital form import
ANTHROPIC_API_KEY=

# Supabase — Postgres + Auth + Storage
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe — per-user/month billing
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=

# Platform admin — comma-separated Supabase auth user UUIDs that get
# read access across every customer org (the /admin pages). Find your
# UUID in Supabase → Authentication → Users.
ADMIN_USER_IDS=
